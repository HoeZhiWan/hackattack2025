{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray",
  "description": "Capability for the system tray",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:tray:default"
  ]
}
